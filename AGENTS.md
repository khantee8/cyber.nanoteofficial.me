# AGENTS.md

This project is Next.js 16 + React 19; APIs differ from most training data. Read `CLAUDE.md` first — it holds the architecture rules (auth gate location, `getDb()` rule, cache choice, catalogue wording rule) and the release gate.
