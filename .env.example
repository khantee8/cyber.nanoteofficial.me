# Neon Postgres (unset is OK for build — only gated pages need it)
DATABASE_URL=

# Auth.js
AUTH_SECRET=            # npx auth secret
AUTH_RESEND_KEY=        # Resend API key (same account as exam.nanoteofficial.me)
AUTH_RESEND_FROM="NaNote Cyber <cyber@nanoteofficial.me>"

# comma-separated emails that skip approval and get role=admin
ALLOWED_EMAILS=khantee9@gmail.com
